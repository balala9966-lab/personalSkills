# Preset Details

Each preset in `references/presets.md` is a Type+Style (sometimes +Palette) shortcut. The summary table there is sufficient for most uses.

To add a per-preset detail page, follow `_template.md`. Detail pages are useful when a preset has non-obvious tuning advice (e.g. specific composition rules, special label conventions).
