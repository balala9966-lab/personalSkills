# Character References

If you swap in a custom IP (instead of using the default Tinkerer), drop a few reference images of that character here. The skill will pick them up from `references/ip-character.md` if you list them.

The default Tinkerer needs no reference images — it is defined purely by the description in `references/ip-character.md`. Adding reference images would over-constrain the style toward one particular rendering.
